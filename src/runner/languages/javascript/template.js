// JavaScript (Node.js) Playground
// Write your JS code here and press F5 or Ctrl+R to run.

console.log("Hello from JavaScript!");

const colors = ["Red", "Green", "Blue"];
colors.forEach((color, index) => {
    console.log(`${index + 1}: ${color}`);
});
